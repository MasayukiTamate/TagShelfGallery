---
name: gazo-tools-development
description: "Use when: fixing bugs in GazoTools, adding image-analysis features, refactoring GUI or logic, running focused tests, or updating documentation for this project"
---

# GazoTools 開発スキル

## 使うとき
- 画像管理アプリのバグ修正をするとき
- GazoToolsApp.py / GazoToolsLogic.py / lib 配下の実装を変更するとき
- 設定値や状態管理の追加・修正をするとき
- テストを実行して回帰を確認するとき
- ドキュメントやマニュアル更新が必要なとき

## プロジェクトの基本方針
このプロジェクトは画像検索・類似画像判定・ベクトル解析を扱うアプリです。実装は次のように分かれています。

- GazoToolsApp.py: アプリの入口、UI 制御、イベント接続
- GazoToolsLogic.py: 画像処理・探索・状態遷移の中心ロジック
- lib/: 共通ライブラリ、GUI コンポーネント、設定、例外、ロガー
- tests/: 回帰確認用の pytest テスト
- docs/: 設計、実装メモ、マニュアル、レポート
- data/: 画像メタデータやベクトル関連のデータ

## 標準ワークフロー

### 1. 問題の切り分け
- まず再現条件を明確にする
- 影響範囲を GazoToolsApp.py, GazoToolsLogic.py, lib 配下から絞る
- 既存のテストがある場合は、最小の対象テストを見つける

### 2. 最小限の確認
- 追加修正前に関連テストを1つだけ実行して現状を確認する
- 変更対象が GUI かロジックかを見極める
- 状態管理や設定ファイルが関係する場合は config_defaults.py を確認する

### 3. 修正方針
- 1つの根本原因に対して 1 回の修正に絞る
- UI とロジックの責務を混在させない
- 設定値追加時は defaults と保存/読み込みの両方を確認する
- 既存の画像処理やベクトル互換性を壊さないようにする

### 4. 検証
- 関連するテストを実行する
- 可能なら最小の pytest 実行で動作確認する
- GUI 的な変更は画面遷移と設定反映の確認を行う
- 変更がユーザー向け仕様なら docs や manual も更新する

## 典型的なタスク別ガイド

### バグ修正時
1. 再現手順を確認する
2. 失敗箇所を GazoToolsApp.py / GazoToolsLogic.py / lib から追う
3. 直前の変更や設定の影響を確認する
4. 根本原因を修正し、最小のテストで検証する
5. 必要に応じてログや docs を更新する

### 機能追加時
1. どの層に責務を持たせるかを決める
   - UI: GazoToolsApp.py または lib/GazoToolsGUI.py
   - 処理: GazoToolsLogic.py
   - 共通部品: lib/
2. 既存の設定構造との整合性を確認する
3. 仕様変更がある場合は manual.md / manual.html / docs 配下も更新する
4. 回帰防止のため関連テストを追加する

### 設定値追加時
- lib/config_defaults.py の既定値を更新する
- セーブ/ロードの処理があるなら対応を追加する
- 既存ユーザー設定との互換性を考慮する
- 未設定時のフォールバック動作を確認する

### ベクトル・画像解析の修正時
- 互換性（既存ベクトル次元や古いデータ）を前提に確認する
- vector interpreter / AI コンポーネントの変換ロジックを優先して検証する
- 画像メタデータ保存と UI 表示の両方を確認する
- 解析結果がユーザーに見える場合は説明可能な表示にする

## 実行コマンドの目安
このプロジェクトでは以下をよく使う。

```bash
pytest tests/test_config.py -q
pytest tests/test_integration.py -q
pytest tests/test_tag_filter.py -q
pytest -q
```

特定の修正に対しては、関連テストを絞って実行するのが基本です。

## ルール
- 既存の設計を壊さない
- ユーザー向けの仕様変更は docs へ反映する
- 変更範囲を広げすぎない
- 例外処理とログの整合を保つ
- テストが存在する箇所では、テストを優先して確認する

## 出力時の基本姿勢
- 修正内容は「何を変えたか」と「なぜそうしたか」を簡潔に説明する
- 失敗した確認結果は隠さず報告する
- 変更が大きい場合は、影響範囲と確認方法を明示する

## 機能分割とテスト再設計の標準フロー

### フェーズ1: 機能ごとの責務分離
1. 既存の大きいモジュールを責務ごとに分ける
   - UI: 表示・ダイアログ・ウィンドウ制御
   - 状態: 設定・保存・復元
   - ファイル: 画像ファイル列挙・ハッシュ・メタデータ
   - AI/ベクトル: ベクトル計算・解釈・類似度評価
   - 画像処理: サムネイル生成・描画・変換
2. ルートにある大きな処理ファイルは、最小限の入口として残す
   - GazoToolsApp.py はアプリ起動とイベント接続を担当
   - GazoToolsLogic.py は統合用の薄いラッパーとして整理する
3. 新しいモジュールは lib/ 配下で責務別に配置する
   - lib/ui/
   - lib/config/
   - lib/files/
   - lib/ai/
   - lib/image/
4. 互換レイヤーを一時的に残す
   - 旧 import を壊さないようにする
   - 1段階ずつ置き換える

### フェーズ2: コード分離の確認
- 1機能ごとに import 依存を見直す
- 循環参照が起きていないか確認する
- GUI とロジックが混ざっていないかを確認する
- 設定値の保存/読み込みが分離されているか確認する

### フェーズ3: テストの再設計
1. ルート直下の古い test 系は legacy/experimental として整理する
2. [tests](tests) を正式な pytest スイートに統一する
3. テストを機能別に分ける
   - test_config.py
   - test_file_operations.py
   - test_ui_state.py
   - test_vector_interpreter.py
   - test_integration.py
4. pytest の自動検出に従って `test_*.py` を配置する
5. fixture と共通セットアップを [tests/conftest.py](tests/conftest.py) に集約する
6. 1ファイル 1責務にし、テスト対象と検証目的を明確にする

### フェーズ4: 検証ルール
- 変更ごとに関連テストを最小単位で実行する
- 例: `pytest tests/test_config.py -q`
- 大きな変更時は `pytest -q` をまとめて実行する
- GUI 変更は UI と設定保存の両方を確認する
- AI / ベクトル変更は互換性とスコア解釈を確認する
- 仕様変更時は docs と manual も更新する

### フェーズ5: 完了判定
- すべての機能が独立モジュールに分かれている
- ルートに散在している実験用テストが整理できている
- [tests](tests) で正式な回帰テストが動く
- import と責務の境界が明確になっている
- 既存の動作を壊していない

## まとめ
このスキルは、GazoTools を安全に改善しながら回帰を防ぐための標準手順です。特に、UI 制御、ロジック、設定、ベクトル解析、ドキュメント更新を同時に扱うこのプロジェクトでは、責務の分離と検証の徹底が最重要です。

今回の方針は、機能分割の完了後にテストを全面的に再設計し、正式な pytest スイートに統一することです。分割の順序と確認のルールを明文化しておくことで、リファクタリング中の破壊的変更を最小化できます。

